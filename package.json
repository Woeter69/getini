{
  "name": "getini",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"frontend,backend\" -c \"blue,green\" \"cd frontend && bun run dev\" \"cd backend && bun run dev\"",
    "build": "cd frontend && bun run build && cd backend && bun run build",
    "install-all": "cd frontend && bun install && cd backend && bun install",
    "backend": "cd backend && bun run dev",
    "frontend": "cd frontend && bun run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
